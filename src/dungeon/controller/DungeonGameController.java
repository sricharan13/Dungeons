package dungeon.controller;

/**
 * Controller for the DungeonGame. Controller should be initialized with
 * a DungeonGame model, and an input source for the controller and an output source to display
 * results.
 */
public interface DungeonGameController {

  /**
   * Plays the game with the given model.
   */
  void playGame();

}
